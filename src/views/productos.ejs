<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Todos los productos</h1>
    <h2><a href="/carrito">Ir al carrito</a></h2>
    <table id="tabla" cellspacing="0" cellpadding="50">
      <thead>
        <tr>
          <th scope="col">Producto</th>
          <th scope="col">Precio</th>
          <th scope="col">Foto</th>
        </tr>
      </thead>
      <% if(todosLosProductos.length===0) { %>
        <tbody id="cajaTbody">
          <tr>
            <td id="no hay datos" colspan="3">No hay datos</td>

          </tr>
        </tbody>
        <% } else { %>
          <% for(let i=0; i<todosLosProductos.length; i++) { %>
            <tr>
              <td align="center">
                <%=todosLosProductos[i].producto %>
              </td>
              <td align="center">
                <%=todosLosProductos[i].precio %>
              </td>
              <td align="center">
                <img src="<%=todosLosProductos[i].imagen%>" height="150px" alt="5">
              </td>
              <td align="center">
                <form action="/productos/addCart" method="post">
                  <button id="id" name="id" value="<%=todosLosProductos[i]._id%>">Agregar al carrito</button>
                </form>
              </td>
            </tr>
            <% }}%>
    </table>
  </body>
</html>
